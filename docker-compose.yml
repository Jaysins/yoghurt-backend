services:
  yoghurt-backend:
    build: .
    container_name: yoghurt_backend
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://postgres:postgres@host.docker.internal:5432/yoghurt
